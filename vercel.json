{
  "version": 2,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "echo 'Skipping build step'",
  "env": {
    "ALLOW_ORIGIN": "*"
  }
}
